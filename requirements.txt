openai>=1.40.0
anthropic>=0.40.0
python-dotenv>=1.0.0
pyyaml>=6.0
# CLI (the PyPI package uses a hyphen; Python imports it as prompt_toolkit).
# Keep this exact so a normal requirements upgrade repairs older/broken installs.
prompt-toolkit==3.0.52
pathspec>=0.12
# 1.27.2 includes Windows executable resolution and process-tree cleanup.
mcp>=1.27.2

# Web search (optional, for web_search tool)
duckduckgo-search>=6.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0

# Web fetch (optional, for web_fetch tool — 正文抽取，按优先级取任一即可)
# trafilatura 效果最好；没装则回退 beautifulsoup4（已在上方）纯文本抽取
trafilatura>=1.6.0
markdownify>=0.11.0
